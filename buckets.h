#ifndef _BUCKETS_H_
#define _BUCKETS_H_

#include <vector>

namespace buckets {
std::vector<std::pair<int, int> > Run(
    int capacity_a, int capacity_b, int target);
}  // namespace buckets

#endif  // _BUCKETS_H_
